if not storage._SHUTDOWN_DATA then
	game.print(
		"WARNING: You may have skipped past a Git release that requires a reset. If this is the case, you must downgrade your Git release, perform the shutdown procedure, save, then re-update to this release and perform the restart procedure."
	)
end
